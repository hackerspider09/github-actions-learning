name: test GITHUB_ENV

on:
  workflow_dispatch

jobs:
  github-env:
    runs-on: ubuntu-latest
    env:
      VERSION: 1.0
    steps:
      - name: create variable
        run: echo "VERSION=1.1" >> $GITHUB_ENV

      - name: print all env
        run: env | sort
